# PharmaGuard — Progress Tracker

Tracks actual build status against [MILESTONES.md](MILESTONES.md). Update this file as work happens — check items off, add notes for anything a future session needs to know.

## Done

- [x] **M0 — Project setup** — Vite + React scaffolded, deps installed, dev server verified. `package.json` name fixed to `pharmaguard`.
- [x] **M1 — Static data & reference content** — `src/data/alerts.json` (13 entries, verbatim from spec). Tips card content built inline in M2's UploadScreen.
- [x] **M2 — Screen 1: Upload UI** — `src/screens/UploadScreen.jsx`, `src/components/ImageUploadSlot.jsx`, front/back thumbnails, submit disabled until front image present.
- [x] **M3 — AI integration layer** — `src/services/ai/` (schema.js, geminiClient.js, openaiClient.js, visionService.js). Gemini primary (`gemini-2.5-flash`) + OpenAI fallback (`gpt-4o`) on failure/10s timeout, provider logged to console. Verified in isolation against real `fake.png`/`real.png`.
- [x] **M4 — Screen 2: Processing UI** — `src/screens/ProcessingScreen.jsx`, wired into `App.jsx`. Shows the 3 exact spec messages, driven by the real API call lifecycle (see note below).
- [x] **M5 — Verification logic** — `src/logic/verifyMedicine.js` + `stringSimilarity.js` (Damerau-Levenshtein). Pure JS, 17 manual test cases passing. Not yet wired to UI.
- [x] Redesigned UI to match user-provided mockup (Sora/Inter fonts, color tokens, card styles) — responsive via one CSS grid with named areas (mobile stacked, desktop 2-column reflow at ≥900px).
- [x] Renamed `.env` keys to `VITE_GEMINI_API_KEY` / `VITE_OPENAI_API_KEY` so Vite exposes them client-side; added `.env` to `.gitignore`.
- [x] Added M11 (Vercel deploy) and M12 (optional Dockerize) to MILESTONES.md.
- [x] **M6 — Screen 3a: Not-a-drug / unreadable** — `src/screens/EarlyExitScreen.jsx` (one component, two variants), wired in `App.jsx` off `verifyMedicine`'s status. Verified `not_a_drug` end-to-end with a real Gemini call against a generated non-drug image. Extracted shared `.btn-primary` class into `index.css`.
- [x] **M7 — Screen 3b: Result screen** — `src/screens/ResultScreen.jsx`, `src/services/ai/explanationService.js` (+ `explainWithGemini`/`explainWithOpenAI` on the existing clients, `explanationPrompt.js`), `src/logic/buildSummary.js`, `src/constants.js` (disclaimer/labels/advice as single source of truth). Wired into `App.jsx` for `confirmed_alert`/`flagged`/`no_flags_found`. Full pipeline (extract → verify → explain → summary) verified end-to-end against real `fake.png`, landing on `flagged`.
- [x] **M9 — End-to-end testing against demo scenarios** (user-requested, also asked to organize test images):
  - Organized all test fixtures into `test-images/` with descriptive names and a documenting `test-images/README.md` (outcome table + notes). Kept a reusable runner at `scripts/test-e2e.mjs` (`node --dns-result-order=ipv4first --env-file=.env scripts/test-e2e.mjs`).
  - **Critical bug found and fixed**: OpenAI (now vision-primary) fabricated a complete fake product — invented name, manufacturer, batch no., NAFDAC reg no. — for a non-drug image and a blurry image, instead of returning `is_drug: false` / nulls. Root cause: no `temperature` set (defaulted to 1.0), plus the anti-hallucination instruction wasn't forceful enough for that provider. Fixed with `temperature: 0` on both providers' extraction calls and a much stricter instruction in `schema.js` ("looks plausible" is never sufficient — only report what's literally legible). Re-verified fixed on both scenarios. This fix is provider-agnostic and stays in effect now that the primary/fallback order has been corrected (see below) — it protects whichever provider actually serves a given vision request.
  - **New capability surfaced by real test data**: the user's `retrax-*.png` photos (Levamisole-based "Retrax Worm Syrup") revealed that active-ingredient-wide recalls (like alert 030/2026) weren't matchable at all — the old logic only matched by batch/reg number or brand-name lookalike, never by active ingredient. Added an `active_ingredient` field to the extraction schema and a new exact-match path in `verifyMedicine.js` (parallel to the existing Otrivin whole-stock special case). Also fixed the explanation prompt, which was hardcoded to claim "batch or registration number matches" even when the real match was via ingredient or product name — now it identifies the actual connecting field.
  - All 7 scenarios pass: AC3N confirmed (synthetic — no real AC3N prop exists), Otrivin confirmed (synthetic), Retrax/Levamisole confirmed (real photo, active-ingredient match), expired+flagged (real photo), an intentionally-ambiguous real composite photo (flagged or no_flags_found, never confirmed/fabricated), not-a-drug, and unreadable/blurry.
  - Known gap: no physical AC3N or Otrivin prop exists yet for the live demo rehearsal — synthetic images validate the pipeline but aren't a substitute. Gemini's 20/day free quota was exhausted by this session's testing; fallback trigger confirmed structurally but not a full successful response today.
- [x] **Alert dataset audit + fixes** (user asked "if any of these drugs are tested, will the trigger work" — audited all 15 alerts at the time by running each one's own ideal data through `verifyMedicine()` directly, not by inspection). Found 6 of 15 had no working trigger at all. Removed 2 with no matchable data and no realistic path to any (028/2026 Children's Ibuprofen, 08/2026 Aptamil — down to 13 entries). Fixed the other 4: added `active_ingredient` to 11/2025 (Chlorpromazine, zero code change, same mechanism as Levamisole); added a dedicated `whole_stock_alert` boolean (rather than overloading `type`) so both Otrivin (019/2026) and ViroActive+ (020/2026) get the whole-product-line match; added batch-prefix matching to `findExactAlertMatch` for 025/2026 (Citro-Soda), which had an unused `batch_prefix` field; made `findBrandLookalike` split comma-separated multi-product fields so all 4 names in 038/2026 are individually reachable, not just the first. Verified with targeted before/after tests (not full vision-API re-runs, per instruction to conserve API quota) — all 20 unit tests and the full real-API e2e suite still pass. Full writeup in `docs/ALERTS.md`.
- [x] **Total vision failure now shows a real message** (user-requested, after I flagged the asymmetry: explanation failures degrade gracefully with a visible note, but a total vision failure — both Gemini keys failing — silently dumped the user back to a blank upload screen with only a `console.error`). Added an `error` variant to `EarlyExitScreen` ("Something went wrong" / "please try again in a moment"), wired `App.jsx`'s catch block to route there instead of silently resetting. Also bounded the previously-unbounded fallback Gemini call with its own 10s timeout (only the primary attempt had one before), so total vision wait is now capped at ~20s either way. Verified for real: started a second dev server instance on a different port with both `VITE_GEMINI_API_KEY`/`VITE_GEMINI_FALLBACK_API_KEY` overridden to invalid values (without touching the real `.env`), drove a real upload + submit via CDP, confirmed the new error screen renders correctly in ~1.5–2.5s.
- [x] **Manual light/dark toggle** (user-requested — dark mode previously only followed the OS setting via `prefers-color-scheme`, with no in-app control): added `ThemeToggle.jsx` in the header (sun/moon icon, animated swap), backed by `document.documentElement.dataset.theme` + `localStorage`. Restructured every `@media (prefers-color-scheme: dark)` block across the CSS (`index.css`, `TipCard.css`, `ImageUploadSlot.css`, `EarlyExitScreen.css`, `ResultScreen.css`) to also respect an explicit `[data-theme]` override, so OS-preference and manual-choice modes coexist correctly instead of the toggle fighting the media query. Added a tiny inline script in `index.html` to apply a saved theme before first paint (avoids flash-of-wrong-theme on reload). BrandHeader's root changed from `<header>` to `<div>` since it's now nested inside `PageLayout`'s own `<header>` — avoids duplicate header landmarks. Verified for real via Chrome DevTools Protocol (no click-automation library installed, so scripted it directly over the CDP WebSocket): confirmed default light state, clicked the actual button, confirmed `--ink` and other CSS custom properties genuinely re-cascaded to dark values, reloaded the page, confirmed the dark choice persisted before React even mounted.
- [x] **M10 — Demo readiness check** — full report in `DEMO_READINESS.md`. Measured real end-to-end timing by actually driving the app in a browser via CDP (real file inputs set, real submit click, real rendered result) rather than a synthetic script: 12.1s for the heaviest scenario (2 real photos). This surfaced a real risk — the pre-fix number was 16.4s with vision alone taking 10.3s on uncompressed photos — so added client-side image compression (`resizeImageForUpload` in `imageUtils.js`, downscale to 1280px + JPEG re-encode, wired into `visionService.js`) before calling it done. Confirmed the exact disclaimer text renders on all 3 result variants via the same real-browser method. Documented prop readiness: Retrax (real photo) ready; AC3N and Otrivin still only have synthetic images, no physical prop yet.
- [x] **M12 — Dockerize** — `Dockerfile` (multi-stage: `node:22-alpine` build → `nginx:alpine` serve), `.dockerignore`, `docker-compose.yml`. Vite inlines `VITE_*` vars at build time, so they're passed as Docker build args (`docker compose build` reads them from `.env` automatically), not left as unused runtime env vars — documented why in the Dockerfile comment. Actually built and ran it (not just wrote the files): confirmed both API endpoints + keys landed correctly in the built bundle, then drove a real end-to-end test against the running container (real file upload, real click) — got the correct `CONFIRMED ALERT / 030/2026` result, confirming the containerized build is functionally identical to the dev server. That run took 25s instead of 12.1s; traced it to Gemini apparently no longer fast-failing on quota (quota may have reset) and instead running to its 10s timeout before falling back to OpenAI — AI-provider variance, not a container/nginx issue (static asset serving was instant per the container logs).
- [x] **Show missing fields on the "unreadable" screen** (user-requested): confirmed the app already skips verification + the AI explanation call entirely when it hits the `unreadable` gate (both `batch_no`/`nafdac_reg_no` missing, or low confidence) — no wasted work, no misleading result. What was missing was specificity: the screen just said "try again," not what to fix. Added `getMissingKeyFields` to `verifyMedicine.js` (checks product_name/batch_no/nafdac_reg_no/expiry_date), wired through `App.jsx` to `EarlyExitScreen`, which now lists exactly which fields it couldn't read. Deliberately did NOT change the `flagged` path (e.g. a single missing reg. no.) to also stop-and-ask — a pack with no NAFDAC reg number printed at all is itself a real safety signal per NAFDAC's own guidance, not just a bad photo, so that stays a flag rather than a retake prompt. Verified end-to-end with the real `blurry-unreadable.png` fixture and a screenshot of the actual rendered screen.
- [x] **Provider order correction, round 1** (user-requested, corrected after an initial misreading): **vision is Gemini-primary / OpenAI-fallback**, **explanation is OpenAI-primary / Gemini-fallback**. (I first implemented this backwards — OpenAI-primary for vision — the user caught it and clarified; both services fixed and re-verified.) Added alert `030/2026` (Levamisole-containing medicines, EU withdrawal over leukoencephalopathy risk) to `alerts.json`, fetched from the NAFDAC source page.
- [x] **Provider order correction, round 2 — final** (user clarified further: "Gemini for vision only, OpenAI for text only, fallback for vision is a Gemini fallback, no fallback for text"): removed OpenAI from vision entirely and Gemini from explanation entirely.
  - `visionService.js`: Gemini primary key → **second Gemini API key** (`VITE_GEMINI_FALLBACK_API_KEY`, already present in `.env` but previously unused/undocumented) as fallback. OpenAI is never called for vision now.
  - `explanationService.js`: OpenAI only, no AI fallback at all. If it fails, `ResultScreen` already falls back to the existing deterministic non-AI `buildFallbackExplanation` — no code change needed there, just removed the Gemini retry attempt.
  - Deleted now-dead code: `extractWithOpenAI` + `buildOpenAiResponseSchema` (OpenAI vision was removed), `explainWithGemini` (Gemini explanation was removed).
  - **Caught a real, unrelated production break while re-verifying**: `gemini-2.5-flash` had been retired by Google mid-session ("no longer available to new users") — every Gemini call was silently failing with a 404 that would have looked like a fallback-key problem if I hadn't checked the actual error body. Updated `GEMINI_MODEL` to `gemini-3.6-flash` per Google's own error message, re-verified both keys work with real calls.
  - `gemini-3.6-flash` is also showing intermittent `503 UNAVAILABLE` ("high demand") responses — confirmed both keys work when it's not under load, and confirmed the primary→fallback retry logic works correctly, but on two of seven scenarios in a single test run, *both* keys hit the 503 within the same few seconds. Made `scripts/test-e2e.mjs` resilient to this (catches per-scenario instead of crashing the whole suite) rather than treating it as a fixable bug — it's Google-side capacity, documented as a pre-demo smoke-test risk in `README.md`.
- [x] **Desktop layout rebuild** (user-requested — desktop looked like "a card floating in empty space," not a real website): removed the shadowed/bordered `#root` card pattern entirely. Built a shared `PageLayout` component (`src/components/PageLayout.jsx`) used by all 4 screens: a full-width header bar + content on the page background, with a `wide` (1160px, for Upload's 2-column hero) vs. narrow (720px reading column, for Processing/EarlyExit/Result) content container. Content is vertically centered in the viewport when shorter than it (hero-like), consistent with real product-page patterns. Verified all 4 screens on both mobile and desktop via headless screenshots, plus dark mode.
- [x] **Motion/polish pass** (post-M8, user-requested): added real interactivity across the app — button hover/active states, `:focus-visible` rings, hover lift on upload slots/tip cards/source links, a shared `screen-enter` fade+slide animation on every screen, staggered step entrance + pop on the processing screen, a verdict-badge pop on the result screen, and a skeleton shimmer replacing the plain "Writing explanation…" text. Verified settled (post-animation) state via headless screenshots at multiple `virtual-time-budget` values, since animations render mid-flight in a zero-delay screenshot.
- [x] **M8 — Styling pass** — visually verified every screen via real headless-Chrome screenshots (mobile + desktop + dark mode), not just CSS review. Found and fixed two real bugs: (1) desktop had a large dead-space gap below the card since `body` forced full-viewport height with no vertical centering; (2) the vertical-centering fix itself broke card width consistency (`display:flex` on `body` made `#root` shrink-to-fit content instead of stretching to `max-width`, so Processing's narrow content produced a much smaller card than Upload/Result) — fixed by adding explicit `width: 100%` to `#root`.

## Notes for continuing work

- **Message pacing tradeoff (M4):** is-drug-check + label extraction are one combined Gemini/OpenAI call (efficiency choice from M3), so message 1 shows for the real call duration, but messages 2→3 are a short cosmetic cascade (600ms floor each) after the call resolves, not tied to a real intermediate event. User confirmed this is fine as-is.
- **Otrivin special case (M5):** alert 019/2026 (`unregistered_all_stock`) has no `batch_no`, so it's matched by product-name instead — this is narrowly scoped to that alert type only. Other alerts missing `batch_no` (e.g. Forxiga) do NOT get this treatment, since genuine stock of those still exists.
- **Explanation prompt hardening (M7):** OpenAI (fallback provider) appended a broken `"Source: nafdac.gov.ng, Public Alert."` (no ID) on `flagged` results despite instructions. Fixed by making the no-citation case a hard "do NOT write the word Source" rule instead of a conditional. Re-verified on both providers.

## Up next (not started)

- [ ] **M11 — Deploy to Vercel**.
